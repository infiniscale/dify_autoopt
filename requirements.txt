loguru>=0.7.0
pydantic>=2.6.0
PyYAML>=6.0.1
ruamel.yaml>=0.17.35
jinja2>=3.1.2
allpairspy>=2.5.0
openpyxl>=3.1.2
python-dotenv>=1.0.1    # Load env vars for CLI/bootstrap
requests>=2.31.0        # HTTP client (auth + LLM health/judge)
APScheduler>=3.10.4     # Scheduler used by auth
tzlocal>=4.3            # Timezone support for schedulers

# Testing
pytest>=7.4.4
pytest-cov>=4.1.0
responses>=0.25.0

# LLM Integration
openai>=1.12.0          # OpenAI GPT API client
anthropic>=0.34.0       # Anthropic Claude API client
tiktoken>=0.5.2         # Token counting for OpenAI models
tenacity>=8.2.3         # Retry logic with exponential backoff

# Optional: dspy-based prompt rewrite (used if installed)
dspy-ai>=2.5.0
